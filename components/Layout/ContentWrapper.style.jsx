import styled from "styled-components";
import { primary } from "../../styles/colors";

const ContentWrapper = styled.div`
  flex: 1;
  background-image: linear-gradient(${primary}, #445777 15%);
  padding: 0.75rem 2rem;
`;

export default ContentWrapper;
